{
  "name": "vujspractice",
  "version": "0.0.0",
  "description": "Vue Practice",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --directory --files='src/**/*.html, src/**/*.css, src/**/*.js'",
    "bower:install": "bower install --save",
    "postbower:install": "wiredep --src src/index.html",
    "sass": "node-sass src/scss/main.scss -o src/css/"
  },
  "author": "",
  "license": "CCO",
  "devDependencies": {
    "browser-sync": "^2.7.12",
    "wiredep": "^2.2.2"
  }
}
